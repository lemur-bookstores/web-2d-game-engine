# Collider — Colisionadores y detección

Ejemplo

```ts
entity.addComponent({ type: "collider", shape: "rect", width: 16, height: 16 });
```

Descripción

- Los `Collider` son procesados por el sistema de colisiones para generar eventos.
