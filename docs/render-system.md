# Render System — Batching, cámaras y renderizado

Ejemplo

```ts
import { RenderSystem } from "../src/graphics/RenderSystem";
engine.addSystem(new RenderSystem());
```

Descripción

- Soporta Canvas2D y una API de batch para WebGL cuando esté disponible.
